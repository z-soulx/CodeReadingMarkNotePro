# TopicLine 行号自动修复功能说明

## 📋 版本信息

- **版本**: v3.3.0
- **发布日期**: 2025-10
- **主要特性**: 智能行号错位检测与修复

---

## 🎯 解决的问题

### 问题描述
当代码发生变化时（切换分支、他人修改代码、拉取远程分支等），TopicLine 记录的行号可能会错位或失效。

### 解决方案
通过与 IDEA 原生 Bookmark 系统绑定，利用 Bookmark 的自动位置跟踪能力，实现智能的行号同步和修复。

---

## ✨ 已实现的功能

### 1. 核心组件

#### ✅ LineOffsetDetector（错位检测器）
- **文件**: `autofix/LineOffsetDetector.java`
- **功能**:
  - 检测单个/批量 TopicLine 的错位状态
  - 支持缓存机制（3秒），避免频繁检测
  - 提供统计信息（总数、正常、错位、丢失等）

#### ✅ OffsetStatus（错位状态枚举）
- **文件**: `autofix/OffsetStatus.java`
- **状态类型**:
  - `SYNCED` - ✅ 已同步
  - `OFFSET` - ⚠️ 已错位
  - `BOOKMARK_MISSING` - ❌ Bookmark 丢失
  - `FILE_MISSING` - 🚫 文件不存在
  - `UNKNOWN` - ❓ 未知状态

#### ✅ AutoFixService（自动修复服务）
- **文件**: `autofix/AutoFixService.java`
- **功能**:
  - 修复单行/整个 Topic/所有 TopicLine
  - 异步修复支持
  - 智能检测并通知
  - 批量操作优化

#### ✅ AutoFixSettings（配置管理）
- **文件**: `autofix/AutoFixSettings.java`
- **配置项**:
  - 是否启用自动修复
  - 自动修复策略（智能模式/自定义/关闭）
  - 通知设置
  - 高级选项（缓存时间、批量阈值等）

### 2. 增强的 Actions

#### ✅ FixLineRemarkAction（修复单行）
- **原功能**: 手动修复单个 TopicLine
- **增强**:
  - 使用新的 AutoFixService
  - 智能检测是否需要修复（只在错位时启用）
  - 显示错位信息（如 "128 → 134 (+6)"）
  - 修复后显示结果对话框

#### ✅ FixTopicRemarkAction（修复 Topic）
- **原功能**: 手动修复 Topic 下所有 TopicLine
- **增强**:
  - 使用新的 AutoFixService
  - 显示错位行数统计
  - 修复后显示结果对话框

#### ✅ FixRemarkAction（修复全部）
- **原功能**: 手动修复所有 TopicLine
- **增强**:
  - 使用新的 AutoFixService
  - 显示详细修复结果
  - 支持错误信息收集

### 3. CodeRemark 显示修复

#### ✅ CodeRemarkEditorManagerListener 增强
- **文件**: `remark/CodeRemarkEditorManagerListener.java`
- **修复的问题**:
  - ❌ 旧问题: 文件打开时使用静态行号，导致错位标记不显示
  - ✅ 新方案: 打开文件前先检测并同步行号
  - ✅ 支持自动检测（可配置）

#### ✅ BookmarkDocumentListener 优化
- **文件**: `remark/BookmarkDocumentListener.java`
- **修复的问题**:
  - ❌ 旧问题: 通过文本匹配 Inlay，不够可靠
  - ✅ 新方案: 优化匹配逻辑，添加 null 检查
  - ✅ 只在行号真正变化时更新

---

## 🚀 使用方法

### 手动修复

#### 方法 1: 修复单行
1. 在 TopicDetailPanel 中右键点击某个 TopicLine
2. 选择 "Fix Line Offset"（仅在错位时可用）
3. 查看修复结果对话框

#### 方法 2: 修复整个 Topic
1. 在 TopicTreePanel 中右键点击 Topic
2. 选择 "Fix Topic Offset"
3. 系统会修复该 Topic 下所有错位的 TopicLine

#### 方法 3: 修复全部
1. 在工具栏或菜单中找到 "Fix All" 按钮
2. 点击执行全局修复
3. 查看修复统计

### 自动修复（配置后）

#### 配置路径
```
Settings > Tools > Code Reading Mark Note Pro > Auto Fix (未来版本)
```

#### 推荐配置
- **启用自动修复**: ✅
- **策略**: 智能模式（推荐）
  - 文件打开时: 自动检测（不修复）
  - 分支切换后: 自动修复全部
  - VCS 更新后: 自动修复全部
- **通知设置**: 
  - 检测到错位时显示通知: ✅
  - 修复前显示确认: ❌
  - 修复后显示结果: ✅

---

## 🔧 技术原理

### 核心机制：Bookmark 绑定

```
用户添加 TopicLine (line 45)
    ↓
创建 Bookmark 并绑定 UUID
    ↓
代码变化（他人在 line 10 插入 5 行）
    ↓
IDEA 自动更新 Bookmark → line 50
    ↓
我们的插件同步 Bookmark 的新行号
    ↓
TopicLine 行号更新为 50 ✅
```

### 为什么依赖 Bookmark？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Bookmark 绑定** | ✅ IDEA 自动维护位置<br>✅ 高准确率<br>✅ 零性能开销 | ⚠️ 依赖外部系统 |
| 内容哈希匹配 | 可以搜索匹配 | ❌ 代码修改后失效<br>❌ 性能差 |
| 差异计算 | 理论最准确 | ❌ 性能极差<br>❌ 存储大 |

### 检测流程

```java
1. 检查文件是否存在
   ↓ 是
2. 检查是否有 bookmarkUid
   ↓ 是
3. 查找对应的 Bookmark
   ↓ 找到
4. 比对 TopicLine.line() vs Bookmark.line()
   ↓
5. 返回状态：SYNCED / OFFSET / BOOKMARK_MISSING / FILE_MISSING
```

### 修复流程

```java
1. 检测错位状态
   ↓
2. 过滤出 OFFSET 状态的 TopicLine
   ↓
3. 可选：显示确认对话框
   ↓
4. 执行修复：topicLine.modifyLine(bookmark.line())
   ↓
5. 清除缓存
   ↓
6. 显示结果通知
```

---

## 📊 性能优化

### 1. 缓存机制
- 检测结果缓存 3 秒
- 避免频繁重复检测
- 数据变更后自动清除缓存

### 2. 批量操作
- 一次性获取所有 Bookmark
- Stream API 并行处理
- 减少 MessageBus 通知次数

### 3. 异步执行
- 自动修复使用线程池
- 不阻塞 UI 线程
- 支持大规模数据处理

---

## 🐛 已修复的问题

### 问题 1: CodeRemark 标记错位
**现象**: 文件打开后，标记显示在错误的行
**原因**: 使用 TopicLine 的静态行号，未同步 Bookmark
**修复**: 文件打开前先同步行号

### 问题 2: CodeRemark 标记不显示
**现象**: 需要关闭文件再重新打开才能看到标记
**原因**: 初始化时行号不一致，Inlay 添加失败
**修复**: 使用实际 TopicLine 数据而不是 CodeRemark 中间层

### 问题 3: BookmarkDocumentListener 更新不准确
**现象**: 文档变化时偶尔更新失败
**原因**: 通过文本前缀匹配不够可靠
**修复**: 优化匹配逻辑，添加 null 检查

---

## 📝 代码文件清单

### 新增文件 (autofix 包)
```
src/main/java/jp/kitabatakep/intellij/plugins/codereadingnote/autofix/
├── OffsetStatus.java          (57 lines)  - 错位状态枚举
├── OffsetInfo.java            (109 lines) - 错位信息类
├── LineOffsetDetector.java    (228 lines) - 错位检测器
├── FixTrigger.java            (50 lines)  - 修复触发器枚举
├── FixResult.java             (155 lines) - 修复结果类
├── AutoFixSettings.java       (273 lines) - 配置管理
└── AutoFixService.java        (289 lines) - 自动修复服务
```

### 修改文件
```
src/main/java/jp/kitabatakep/intellij/plugins/codereadingnote/
├── actions/
│   ├── FixLineRemarkAction.java      (增强：智能检测、结果对话框)
│   ├── FixTopicRemarkAction.java     (增强：统计信息、结果对话框)
│   └── FixRemarkAction.java          (增强：使用新服务、结果对话框)
├── remark/
│   ├── CodeRemarkEditorManagerListener.java (修复：同步行号、自动检测)
│   └── BookmarkDocumentListener.java        (优化：匹配逻辑、null 检查)
└── resources/META-INF/
    └── plugin.xml                     (注册：AutoFixSettings 服务)
```

---

## 🎓 开发者指南

### 如何使用 AutoFixService

```java
// 1. 获取服务实例
AutoFixService service = AutoFixService.getInstance();

// 2. 修复单行
FixResult result = service.fixLine(project, topicLine);

// 3. 修复 Topic
FixResult result = service.fixTopic(project, topic);

// 4. 修复全部
FixResult result = service.fixAll(project);

// 5. 异步修复
CompletableFuture<FixResult> future = service.autoFixAsync(project, FixTrigger.BRANCH_SWITCHED);
future.thenAccept(result -> {
    // 处理结果
});
```

### 如何使用 LineOffsetDetector

```java
// 1. 获取检测器实例
LineOffsetDetector detector = LineOffsetDetector.getInstance();

// 2. 检测单行
OffsetInfo info = detector.detectOffset(project, topicLine);

// 3. 检测所有
Map<TopicLine, OffsetInfo> offsetMap = detector.detectAll(project);

// 4. 获取统计
LineOffsetDetector.OffsetStatistics stats = detector.getStatistics(project);
System.out.println(stats.toString()); // Total: 127 | ✅ 98 | ⚠️ 15 | ❌ 8 | 🚫 6
```

---

## 🔮 未来计划

### Phase 1 (已完成 - v3.3.0)
- ✅ 核心检测和修复功能
- ✅ 增强现有 Fix Actions
- ✅ CodeRemark 显示修复
- ✅ 基础配置管理

### Phase 2 (计划中 - v3.4.0)
- ⏳ UI 增强（状态图标、颜色标识）
- ⏳ 新的 Actions (DetectAllOffset, NavigateToNewPosition, ReCreateBookmark)
- ⏳ ManagementPanel 工具栏和状态栏
- ⏳ Settings 配置页面

### Phase 3 (计划中 - v3.5.0)
- ⏳ 分支切换监听器
- ⏳ VCS 更新监听器
- ⏳ 自动修复（智能模式）
- ⏳ 确认对话框

### Phase 4 (远期 - v3.6.0)
- ⏳ 智能内容匹配恢复（Bookmark 丢失时）
- ⏳ 修复历史记录
- ⏳ 批量操作 UI
- ⏳ 导出修复报告

---

## ❓ 常见问题

### Q1: 修复后还是显示错误的行？
**A**: 可能是缓存问题，尝试：
1. 关闭并重新打开文件
2. 点击 "Fix Line Offset" 再次修复
3. 检查 Bookmark 是否还存在

### Q2: 老数据（没有 bookmarkUid）怎么办？
**A**: 
1. 系统会标记为 UNKNOWN 状态
2. 需要重新 "Add to Topic"
3. 或者使用未来版本的智能匹配功能

### Q3: 文件被删除了怎么办？
**A**: 
1. 系统会标记为 FILE_MISSING 状态
2. 可以选择删除该 TopicLine
3. 或者等待文件恢复后再修复

### Q4: Bookmark 丢失了怎么办？
**A**: 
1. 系统会标记为 BOOKMARK_MISSING 状态
2. 可以使用 "Re-create Bookmark" 重建（未来版本）
3. 或者尝试智能匹配恢复（未来版本）

---

## 📧 反馈与支持

- **GitHub Issues**: [提交问题](https://github.com/your-repo/issues)
- **Email**: 170918810@qq.com
- **Plugin Page**: https://plugins.jetbrains.com/plugin/24163

---

**感谢使用 Code Reading Mark Note Pro！**

*文档最后更新: 2025-10-25*

