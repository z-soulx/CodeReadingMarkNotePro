# Idea
.idea
*.iml
*.iws
*.ipr

# Gradle
.gradle
!gradle-wrapper.jar

/build

# .ai: contract layer is [Git]; runs/ (execution evidence) and local secrets are ignored
/.ai/runs/
/.ai/token.txt
# The AI workspace has its own repository; never add its internal metadata to the plugin repo.
/.ai/.git/
